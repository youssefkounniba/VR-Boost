# Public base URL of the deployment — used to resolve canonical / Open Graph URLs.
# Local dev can omit this (falls back to http://localhost:3000).
# On Vercel, set this to your production domain, e.g. https://vr-boost-agency.vercel.app
NEXT_PUBLIC_SITE_URL=https://vr-boost-agency.vercel.app
